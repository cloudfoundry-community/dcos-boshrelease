---
name: bootstrap

packages:
- dcos
templates:
  bin/pre-start: bin/pre-start
  bin/post-start: bin/post-start
  bin/post-deploy: bin/post-deploy
  genconf/config.yaml: genconf/config.yaml
  genconf/ip-detect: genconf/ip-detect
  genconf/ssh_key.erb: genconf/ssh_key
  helpers/ctl_setup.sh: helpers/ctl_setup.sh
  helpers/ctl_utils.sh: helpers/ctl_utils.sh

consumes:
- name: mesos-master
  type: mesos-master

- name: mesos-agent
  type: mesos-agent

provides: []

properties:
  private_key:
    description: "Private key to ssh into dcos vms"
  cluster_name:
    description: "Name of cluster"
    default: "dcos"
  extra_config:
    description: "Extra config for config file"
